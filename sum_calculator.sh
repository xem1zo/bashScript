#!/usr/bin/env bash

read -p "Введите первое число: " a
read -p "Введите второе число: " b

if ! [[ "$a" =~ ^-?[0-9]+([.][0-9]+)?$ ]] || ! [[ "$b" =~ ^-?[0-9]+([.][0-9]+)?$ ]]; then
  echo "Ошибка: нужно вводить только числа."
  exit 1
fi

sum=$(echo "$a + $b" | bc)
echo "Сумма: $sum"

